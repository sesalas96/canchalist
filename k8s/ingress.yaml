kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: cci-test-ingress
  namespace: dev
spec:
  ingressClassName: alb
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cci-test-service
                port:
                  number: 80
